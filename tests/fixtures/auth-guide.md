# Authentication Guide

Users can authenticate with OAuth2 bearer tokens.

The login flow validates the access token before loading the user profile.

Refresh tokens are rotated after each successful login.
